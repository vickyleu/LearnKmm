name: geyan_plugin
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: lib_bridge
        pluginClass: GeyanHybridPlugin
      ios:
        pluginClass: GeyanHybridPlugin